# Workspace

This is the ppt-mate **workspace** directory. Put assets the agent should be able
to use here: generated images, PDFs used as context, notes, and other user files.

The agent always sees the file tree (a lightweight manifest is sent with every
chat turn) but reads a file's contents only when it needs them. Drop files in here
and ask the agent about them.
